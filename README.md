# Apollo v2

## Installation

```bash
make install-deps
```

## Build

```bash
make build
```

Le build est disponible dans le dossier `dist/`.

## Run en local

```bash
make run-local
```

Ouvre ensuite http://localhost:5173/ dans ton navigateur.
